<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.myviewapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="n">
        <activity android:name=".viewbanner.TestMainActivity"/>
        <activity android:name=".viewbanner.VideoBannerActivity"
            android:theme="@style/Theme_NoActionBar"/>
        <activity android:name=".propertyanimation.objectanimator.ObjectAnimatorActivity" />
        <activity android:name=".kotlin.KotlinTestActivity" />
        <activity android:name=".propertyanimation.valueanimator.ValueAnimatorActivity" />
        <activity android:name=".viewanimation.AnimationCodeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BaseDrawActivity" />
        <activity android:name=".viewanimation.AnimationTestActivity" />
    </application>

</manifest>